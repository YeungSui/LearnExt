//Ext.grid.ColumnMode
//ColumnMode可以设置网格的列的属性，包括列宽、列标题、列排序等等
/*有一下两个主要参数：
	default:{...} 定义默认的列属性，比如多数列的宽度都是相同的可以用default:{width:'100'}
	columns:[{...},{...}] 单独定义每个列的属性，比如列标题 header、数据id(注意对应record的field name)dataIndex等
*/